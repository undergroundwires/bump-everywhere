name: Test

on:
  pull_request:
    branches:
      - master

jobs:

  build-docker:
    name: Build docker
    runs-on: ubuntu-latest
    steps:
      - 
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Build docker
        run: docker build . --tag bump-everywhere